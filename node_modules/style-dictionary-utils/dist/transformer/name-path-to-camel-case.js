"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.namePathToCamelCase = void 0;
const capitalize = ([firstLetter, ...restOfWord]) => {
    return firstLetter.toUpperCase() + restOfWord.join("");
};
const camelCase = (string) => {
    return string
        .split(/[\s-_\+]+/g)
        .map((part, index) => index === 0 ? part : capitalize(part))
        .join("");
};
/**
 * namePathToCameCase
 * @description convert the entire `path` to camel case and replaces the name
 */
exports.namePathToCamelCase = {
    type: `name`,
    transformer: (token, platform) => {
        return camelCase([platform === null || platform === void 0 ? void 0 : platform.prefix, ...token.path]
            .filter((part) => typeof part === 'string')
            .join("-"));
    }
};
